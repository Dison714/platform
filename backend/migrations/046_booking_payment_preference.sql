-- =====================================================================
-- MDB PLATFORM — DATABASE SCHEMA  (Opus build)
-- 046_booking_payment_preference.sql
-- Удобный клиенту способ оплаты — чекбоксы на сайте (наличные / банковский
-- перевод / другое), несколько сразу. Информационное поле для менеджера,
-- на цену и логику брони не влияет.
-- =====================================================================

ALTER TABLE bookings ADD COLUMN payment_preference TEXT[];
COMMENT ON COLUMN bookings.payment_preference IS 'Отмеченные клиентом способы оплаты: cash, bank_transfer, other (можно несколько). NULL = не указал. Только для менеджера — не влияет на бронирование.';
