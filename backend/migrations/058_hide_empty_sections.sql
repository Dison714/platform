-- Route/Distances без реальных данных не должны показываться посетителю
-- вообще — ни плейсхолдер, ни "pending"-текст (замечено Дмитрием при
-- ревью 2026-09-10). Distances уже NULL у 8 районов (фронтенд теперь не
-- рендерит секцию при пустом массиве, без текстового фолбэка — см.
-- [locationSlug]/page.js). route_html хранил TODO-текст как реальные
-- данные на всех 9 — обнуляем везде, фронтенд прячет секцию целиком при
-- NULL. Смысл TODO (анкета водителям не отправлена) остаётся в комментах
-- миграций 052/055, не теряется, просто не показывается публично.
UPDATE location_page_translations SET route_html = NULL WHERE language_code = 'en';
