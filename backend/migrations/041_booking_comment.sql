-- =====================================================================
-- MDB PLATFORM — DATABASE SCHEMA  (Opus build)
-- 041_booking_comment.sql
-- Свободный комментарий клиента (время доставки и прочее) — попадает
-- целиком в заявку менеджеру и в заявку водителю (поле Komentar).
-- =====================================================================

ALTER TABLE bookings ADD COLUMN comment TEXT;
COMMENT ON COLUMN bookings.comment IS 'Свободный текст клиента ("укажите желаемое время доставки, прочую информацию"). НЕ парсится автоматически — время доставки диспетчер читает и проставляет вручную в заявке водителю. Полностью прокидывается в обе Telegram-карточки (менеджер + водитель).';
