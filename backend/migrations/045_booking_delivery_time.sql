-- =====================================================================
-- MDB PLATFORM — DATABASE SCHEMA  (Opus build)
-- 045_booking_delivery_time.sql
-- Точное время доставки — отдельное поле, клиент указывает time picker'ом
-- на сайте. Раньше время просили писать в comment, диспетчер вычитывал
-- вручную ("jam — lihat Komentar" в заявке водителю).
-- =====================================================================

ALTER TABLE bookings ADD COLUMN delivery_time TIME;
COMMENT ON COLUMN bookings.delivery_time IS 'Желаемое время доставки (HH:MM), указанное клиентом на сайте. Попадает в заявки менеджеру и водителю. NULL = не указано (клиент мог написать время в comment вместо этого — старый флоу).';
